import React, { useState } from 'react'

const Tour = ({id,image,info,name}) => {
  return (
    <article className='single-tour'>
      <img src={imege} alt="" />
      
    </article>
  )
}

export default Tour
